# Cabaret AI — 本番稼働セットアップ手順

このアプリ（`cabaret-app.html` = デプロイ時の `index.html`）は、
Supabase の接続情報を設定するだけで「ローカルデモ」から「実際に数名で
安全に使える本番モード」に切り替わります。設定しなければ、これまで通り
ダミーデータのデモとして動きます。

**本番モードで守られること**
- パスワードは平文で保存・比較されません（Supabase Authが管理）
- 住所などの個人情報は「本人」と「店長」以外は読めません（DBレベルで強制）
- 出勤予定は全員がリアルタイムで共有できます（誰かが編集すると全員の画面に即反映）
- ID・パスワードの発行/変更はアプリ画面からはできません（Supabaseの管理画面のみ）＝
  もしアプリのコードや通信を覗かれても、パスワードを盗む/書き換える手段がありません

---

## 1. Supabaseプロジェクトを作成

1. https://supabase.com にアクセスし、アカウント作成（無料枠でOK）
2. 「New project」→ リージョンは **Tokyo (ap-northeast-1)** を選択（データを日本国内に置くため）
3. プロジェクト名・DBパスワードを設定して作成（数分かかります）

## 2. スキーマを適用

1. 左メニュー「SQL Editor」を開く
2. このリポジトリの `supabase-schema.sql` の中身を全部貼り付けて実行（RUN）
3. エラーが出なければOK。これで以下が作られます：
   - `profiles`（名前・役割）
   - `cast_profiles`（住所・エリア＝個人情報。本人と店長しか読めない）
   - `schedules`（出勤予定。全員が読める／書けるのは本人か店長のみ）
   - リアルタイム同期（複数端末で即時反映）

## 3. ログインユーザーを作成（店長＋キャストそれぞれ）

**メールアドレスの決め方**：Supabase Authはメール形式が必要ですが、実在しなくて
構いません（確認メールは送りません）。ここでは
`<ログインID>@cabaret-os.internal` という形式を使う前提で説明します
（`cabaret-app.html` 内の `AUTH_DOMAIN` と同じ値にしてください）。

1. 左メニュー「Authentication」→「Users」→「Add user」
2. Email: `admin@cabaret-os.internal`（店長用の例）、Password: 強いパスワードを設定
3. **「Auto Confirm User」に必ずチェック**を入れて作成（確認メールが送れないため）
4. 作成されたユーザーの **UUID** をコピーしておく
5. キャストの人数分、同様に `aya@cabaret-os.internal` のように繰り返す

## 4. プロフィール行を登録（役割・名前を紐付け）

「SQL Editor」で、3で控えたUUIDを使って1回だけ実行：

```sql
insert into public.profiles (id, role, name, line_name) values
  ('ここに店長のUUID', 'boy',  '店長', null),
  ('ここにあやのUUID', 'cast', 'あや', 'あや★'),
  ('ここにみおのUUID', 'cast', 'みお', 'mio_cabaret');
  -- 人数分続ける
```

これで各ユーザーが「店長」か「キャスト」かをアプリが認識できるようになります。
（キャストの住所は登録不要です。本人が初回ログイン時にアプリ内で入力します）

## 5. 接続情報をアプリに設定

1. Supabase「Settings」→「API」を開く
2. **Project URL** と **anon public key** をコピー
   （※ anon keyは「公開しても良い」設計の値です。RLSで保護されているため、
   このキーだけでは他人のデータは読み書きできません）
3. `cabaret-app.html` の先頭付近、次の3行を書き換える：

```js
const SUPABASE_URL='https://xxxxxxxx.supabase.co';
const SUPABASE_ANON_KEY='ここにanon public keyを貼る';
const AUTH_DOMAIN='cabaret-os.internal'; // 3で使ったドメインと揃える
```

保存すると自動的に「本番モード」になります。

## 6. Vercelへデプロイ

1. GitHubの `cabaret-os` リポジトリを **Private に変更**しておく（推奨。
   Vercelはprivateリポジトリでも接続してデプロイできます）
2. Vercelダッシュボード →「Add New」→「Project」→ `cabaret-os` リポジトリを選択してImport
   （設定変更は不要。静的ファイルなのでビルドコマンドは空でOK）
3. Deployを押すと、`https://（プロジェクト名）.vercel.app` のようなURLが発行されます
4. 以後、`cabaret-os` リポジトリに変更をpushすると自動的に再デプロイされます

iPhoneでは発行されたVercelのURLをSafariで開き、共有ボタン→「ホーム画面に追加」で
これまで通りアプリ化できます。

## 7. 動作確認

- 店長アカウントでログイン → 予定タブ・アカウントタブが表示されるか
- キャストアカウントでログイン → 初回は住所登録モーダルが出るか、保存後に反映されるか
- 2つの端末（またはブラウザ）で同時にログインし、片方で予定を追加 → もう片方に自動で反映されるか（リアルタイム同期の確認）

## まだ本番化していない部分（次のフェーズ）

- **LINE連携**：現状、LINE受信/通知/出勤連絡チャットはブラウザ内のデモのままです。
  実際のLINE公式アカウント連携（Messaging API + Webhook）は今回スコープ外にしています。
- **AI解析（Claude API）**：メッセージの日時解析は現状シンプルな文字列パターンマッチです。
  本格運用時はサーバー側でClaude APIを呼ぶ形に置き換えます（APIキーは絶対にブラウザに置きません）。
- **Web Push（アプリを閉じていても届く通知）**：現状は「アプリを開いている間」の通知のみです。
  閉じていても届く通知には、VAPICキー＋サーバー側の送信処理が必要です。

これらは、今回の「まず数名で安全に使える状態にする」が済んでから、必要になったタイミングで
段階的に追加していく想定です。
